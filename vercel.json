{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/*.py": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "cases/**",
      "excludeFiles": "{src,dist,scripts,public,node_modules,tests,.github,.git,.vercel}/**"
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
